# VSEC

VSEC practical 

git config --global user.email "email"

git config --global user.name "name"

git status

code .

git clone

git add .

git commit -m "message"


git branch

git branch branchname

git checkout branchname

code .


git add .

git commit -m "another msg"

git push

git push -u origin vsecpractical2

git branch -m for rename

git branch -d for deletion
